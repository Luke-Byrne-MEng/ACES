git clone -b [YOUR_BRANCH_NAME] https://github.com/Luke-Byrne-uni/ACES

cd ACES

# copy your .gazebo, catkin_ws etc into ACES folder (whatever you want to add to github)

git status

git add [WHATEVER_WAS_IN_THE_GIT_STATUS]

git commit -m "[DESCRIBE_YOUR_CHANGES]"

git remote

# normally this will reply "origin"

git push [OUTPUT_OF_GIT_REMOTE, NORMALLY "origin"] [YOUR_BRANCH_NAME]
